Run the script(s) by invoking

python get_and_ass_kuplett.py <USERNAME> <PASSWORD> <OUTPUT ASS FILE>
python get_and_inputsong_kuplett.py <USERNAME> <PASSWORD> <OUTPUT DIR>

The latter command will populate subdirs
Akt1
Akt2
Akt3
Omstarter
with files specified in the get_and_inputsong_kuplett.py file, which generally is the file one should modify.

Requires selenium for python (http://www.seleniumhq.org/):
(sudo) pip install selenium
